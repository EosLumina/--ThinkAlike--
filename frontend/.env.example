# ThinkAlike Frontend Environment Variables Example
# Copy this file to .env in the frontend/ directory for local development.
# Create React App uses variables prefixed with REACT_APP_
# DO NOT COMMIT .env to Git - ensure frontend/.env is in .gitignore!

# The base URL of the ThinkAlike Backend API
REACT_APP_BACKEND_URL=http://localhost:8000

# Example: If using a third-party service directly from frontend (less common)
# REACT_APP_SOME_CLIENT_ID=your_public_client_id

# Example: Feature flags controlled via environment variables at build time
# REACT_APP_FEATURE_NEW_PROFILE_VIEW=true

# Note: Only variables starting with REACT_APP_ are embedded by default.
# Other variables might be used by build scripts or Node.js environment directly.
